import React from "react";
import TodoList from '../components/todoList';

const Todo = () => {
    return (
        <div className="todo-panel">
            <TodoList />
        </div>
    );
};

export default Todo;